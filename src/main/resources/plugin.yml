name: ${project.name}
main: ${project.groupId}.${project.artifactId}.UHC
version: ${project.version}
author: ghowden/Eluinhost
database: false
commands:
  showhealth:
    permission: uhc.command.showhealth
  ghead:
    permission: uhc.command.ghead
  addons:
    permission: uhc.command.addons
  border:
    permission: uhc.command.border
permissions:
  uhc.command.showhealth:
    default: op
  uhc.command.ghead:
    default: op
  uhc.commands.addons:
    default: true
  uhc.commands.border:
    default: op